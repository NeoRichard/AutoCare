<!DOCTYPE html>
<html>

  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "AutoCareLight web page" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "AutoCareLight web page" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>


  <body class="<%= controller_name %> <%= action_name %>">
    <div class="navbar navbar-fixed-top">
      <nav class="navbar-inner">
        <div class="container">
          <%= render 'layouts/navigation' %>
        </div>
      </nav>
    </div>


    <div id="main" role="main">
      <div class="container">
        <div class="content">
           <div class="row">
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </div>

<br />
<br />

        </div>
      </div> <!--! end of .container -->
    </div> <!--! end of #main -->
  </body>

          <footer>
            <ul>
              <li>
<%= link_to "Quienes Somos?", root_path, :class => 'footer' %>
              <li>
              </li>
<%= link_to "FAQ", root_path, :class => 'footer' %>
              <li>
              </li>
<%= link_to "Condiciones de Uso", root_path, :class => 'footer' %>
              <li>
              </li>
<%= link_to "Ayuda", root_path, :class => 'footer' %>
              </li>
            </ul>
          </footer>
</html>
